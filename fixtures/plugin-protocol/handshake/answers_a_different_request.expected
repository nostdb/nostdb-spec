outcome = reject
code = PLUGIN_FAILED
note = A plugin answering an invoke before agreeing a version has broken the protocol, which is its failure rather than a complaint about the request it was sent.
