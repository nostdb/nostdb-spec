outcome = reject
rule = second_session_on_one_connection
note = Section 6.1: a connection carries one session. Opening another would silently widen the connection, and replacing the first would discard a session the client still believes in.
